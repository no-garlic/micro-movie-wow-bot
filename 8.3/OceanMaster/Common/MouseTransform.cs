using System;
using CommonLib;

namespace OceanMaster
{
    public class MouseTransform
    {
        // class to take a mouse info file and rotate / scale it

        // need a class to catalog all mouse info files int a database
        //
        // - calc the bounding box of mouse info
        // - reject the mouse info based on bounding box shape or duration
        //   - or store it as an emote movement that is ramdomly played
        //
        // - provide a search function for (xy to xy)
        // - need to determine if it will clip past the screen bounds
        // - ideally find a movement that is close to the correct distance
        //
        // - need to randomize which ones are used        



    }
}
